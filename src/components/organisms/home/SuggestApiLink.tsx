import "twin.macro";
import { BaseTemplate } from "src/components/templates/BaseTemplate";

export const SuggestApiLink = () => {
  return (
    <BaseTemplate>
      <p>！APIが連携されていません！</p>
    </BaseTemplate>
  );
};
