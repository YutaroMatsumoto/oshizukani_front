import "twin.macro";

export const Footer = () => {
  const auth = false; // 暫定対応

  return (
    <footer tw="bottom-0 flex items-center justify-center w-full h-12 bg-white">
      fotterです
    </footer>
  );
};
